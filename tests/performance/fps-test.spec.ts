import { describe, it, expect } from 'vitest';

// Placeholder suite: real FPS performance tests to be implemented
// Skipped to avoid empty test file failures in CI.
describe.skip('Performance: FPS metrics (TODO)', () => {
  it('placeholder - to be implemented', () => {
    expect(true).toBe(true);
  });
});